import 'package:flutter/material.dart';

class view_parts extends StatelessWidget {
  const view_parts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}